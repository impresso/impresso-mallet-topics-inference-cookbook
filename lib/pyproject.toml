[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "impresso-mallet-lda"
version = "0.1.0"

[tool.setuptools]
packages = ["impresso_mallet_lda"]
package-dir = {"" = "lib"}
